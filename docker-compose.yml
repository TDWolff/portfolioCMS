version: '3'
services:
        web:
                image: portfoliocms
                build: .
                ports:
                        - "8454:8454"
                volumes:
                        - ./instances:/app/instances
                restart: unless-stopped